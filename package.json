{
  "name": "netlify-mcp-gateway",
  "version": "0.2.0",
  "type": "module",
  "description": "Unified MCP Gateway deployed on Netlify Edge Functions",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "lint": "deno lint src/ netlify/",
    "format": "deno fmt src/ netlify/",
    "test": "deno test"
  },
  "devDependencies": {
    "@netlify/blobs": "^8.1.0",
    "@netlify/edge-functions": "^2.11.0",
    "netlify-cli": "^23.13.1"
  }
}
